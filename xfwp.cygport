inherit xorg

NAME="xfwp"
VERSION=1.0.2
RELEASE=1
CATEGORY="X11"
SUMMARY="X firewall proxy"
DESCRIPTION="The X firewall proxy (xfwp) is an application layer gateway proxy
that may be run on a network firewall host to forward X traffic across the
firewall."
